= simple_form_for(@service, html: {class: 'form-horizontal'}) do |f|
  = f.error_notification

  .form-inputs
    = f.input :title
  - if @service.persisted?
    .col-lg-6
      label Text
      = f.input :text, input_html: {class: 'editor', data: {editor: 'ruby'}}, wrapper: :nowrapper

      label Attachments
      = f.input :attachments, input_html: {class: 'editor', data: {editor: 'ruby'}}, wrapper: :nowrapper

      label Helper
      = f.input :helper_methods, input_html: {class: 'editor', data: {editor: 'ruby'}}, wrapper: :nowrapper

    .col-lg-6
      label Sample Params
      = f.input :sample_params, input_html: {class: 'editor', data: {context: @service.sample_params, editor: 'json'}}, wrapper: :nowrapper

  .form-actions
    = f.button :submit, value: 'Test', class: 'btn btn-primary pull-right'
